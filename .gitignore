__pycache__/
*.pyc

# Raw videos are large and should be archived outside normal git.
data/*.mp4

# Model weights exceed normal GitHub limits. Put them in models/ locally.
models/*.pt

# Keep JSON/JSONL reports, ignore rendered videos.
outputs/*_output.mp4

.DS_Store
Thumbs.db
